version: '3'

tasks:
  default:
    desc: List all available tasks
    cmd: task --list
  run-dev:
    desc: Run API in development mode
    cmd: uv run fastapi dev backend/main.py
  test:
    desc: Run tests
    cmd: uv run pytest tests/test-system.py
